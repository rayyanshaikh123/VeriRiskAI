from typing import Dict, List


class TemporalDetector:
    def detect(self, frames: List[bytes]) -> Dict[str, float]:
        """Return temporal consistency score (stub)."""
        return {"score": 0.5}
